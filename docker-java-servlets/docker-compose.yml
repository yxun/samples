
services:
  serve:
    build: .
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    volumes:
      - ./src:/usr/local/tomcat/webapps
    ports:
      - "8080:8080"
    environment:
      APP_NAME: 'helloservlet'
      APP_CLASS: 'mypkg'
    command: [ "run.sh" ]
